#include "control.hpp"
/*
Control::Control(std::vector<short int>& listen_port, short int& ws_port)
    : ws_server(ws_port), api_handler(listen_port) {
  listen_port = listen_ports;
  ws_port = ws_port;
}
Control::~Control() {}

void Control::selfcontrol() {
  while (ws_server.id_socket != -1) {

  }
}
*/